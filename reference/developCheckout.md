# \`developCheckout()\` is a wrapper for gert functions to swiftly default to develop and pull latest changes

\`developCheckout()\` is a wrapper for gert functions to swiftly default
to develop and pull latest changes

## Usage

``` r
developCheckout()
```

## Value

Git log messages after checking out and pulling 'develop'
