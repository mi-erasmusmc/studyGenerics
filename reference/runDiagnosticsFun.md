# Load default content for runDiagnostics.R

Load default content for runDiagnostics.R

## Usage

``` r
runDiagnosticsFun(path = ".")
```

## Arguments

- path:

  Character string identifying the path to the study project, default
  `"."`.

## Value

A character vector containing the default contents of
`runDiagnostics.R`.
