# Load default content for createCohorts.R

Load default content for createCohorts.R

## Usage

``` r
createCohortsFun(path = ".")
```

## Arguments

- path:

  Character string identifying the path to the study project, default
  `"."`.

## Value

A character vector containing the default contents of `createCohorts.R`.
